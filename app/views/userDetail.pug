//- extends layout

//- block content
//-   h1 User Details

//-   if user
//-     p ID: #{user.id}
//-     p Name: #{user.name}
//-     p Category: #{user.category}
//-   else
//-     p User not found

extends layout

block content
  if error
    h2.error-message= error
  else
    h1= user.name
    p Email: #{user.email}
    p Category: #{user.category}
    p Details: #{user.details}
    a(href="/users") ← Back to List

